<h1>Editing families</h1>

<% form_for(@family) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => f -%>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Back', families_path %>
